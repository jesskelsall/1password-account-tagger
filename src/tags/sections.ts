import { Section } from './types'

// Add your own sections here to group tags
// Sections are shown in the order they are defined
// Tags not in a section are shown last in an "Other Tags" group
// Tags with a section that isn't defined here will not be grouped either
export const sections: Section[] = [
  {
    name: 'Contact Methods',
    value: 'contact-methods',
  },
]
